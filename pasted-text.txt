<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AUTO SHOP NAME — Trusted Auto Repair in [Neighborhood Name]</title>
  <style>
    /* ─── Reset & Custom Properties ─────────────────────────────────── */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --accent:        #B91C1C;
      --accent-dark:   #991B1B;
      --accent-glow:   rgba(185, 28, 28, 0.45);
      --bg-base:       #0C0C0C;
      --bg-section:    #111111;
      --bg-alt:        #0F0F0F;
      --bg-card:       #181818;
      --border:        rgba(255,255,255,0.07);
      --border-hover:  rgba(185,28,28,0.35);
      --text-primary:  #FFFFFF;
      --text-secondary:#A1A1AA;
      --text-muted:    #52525B;
      --radius:        6px;
      --nav-h:         64px;
      --mobile-bar-h:  70px;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica Neue, Arial, sans-serif;
      background: var(--bg-base);
      color: var(--text-primary);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }

    img { display: block; max-width: 100%; }
    a   { color: inherit; }

    /* ─── Utility ────────────────────────────────────────────────────── */
    .container {
      max-width: 1180px;
      margin: 0 auto;
      padding: 0 2rem;
    }

    .eyebrow {
      font-size: 0.68rem;
      font-weight: 700;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 0.75rem;
    }

    .section-title {
      font-size: clamp(2rem, 4vw, 2.75rem);
      font-weight: 800;
      letter-spacing: -0.025em;
      line-height: 1.1;
      margin-bottom: 0.9rem;
    }

    .rule {
      width: 40px;
      height: 3px;
      background: var(--accent);
      margin-bottom: 3rem;
    }

    /* ─── Buttons ────────────────────────────────────────────────────── */
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      padding: 0.85rem 2rem;
      font-size: 0.85rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      text-decoration: none;
      border-radius: var(--radius);
      border: 2px solid transparent;
      transition: background 0.2s, border-color 0.2s, transform 0.15s, box-shadow 0.2s;
      cursor: pointer;
      white-space: nowrap;
    }

    .btn-primary {
      background: var(--accent);
      color: #fff;
      border-color: var(--accent);
    }
    .btn-primary:hover {
      background: var(--accent-dark);
      border-color: var(--accent-dark);
      transform: translateY(-1px);
      box-shadow: 0 8px 24px var(--accent-glow);
    }

    .btn-ghost {
      background: transparent;
      color: #fff;
      border-color: rgba(255,255,255,0.25);
    }
    .btn-ghost:hover {
      border-color: rgba(255,255,255,0.65);
      background: rgba(255,255,255,0.06);
      transform: translateY(-1px);
    }

    /* ─── Navigation ─────────────────────────────────────────────────── */
    .nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 500;
      height: var(--nav-h);
      background: rgba(12,12,12,0.96);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
      display: flex;
      align-items: center;
      padding: 0 2rem;
    }

    .nav-inner {
      width: 100%;
      max-width: 1180px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .nav-logo {
      font-size: 1rem;
      font-weight: 800;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      text-decoration: none;
      color: var(--text-primary);
    }
    .nav-logo em { color: var(--accent); font-style: normal; }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }
    .nav-links a {
      font-size: 0.8rem;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      text-decoration: none;
      color: var(--text-secondary);
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--text-primary); }

    .nav-cta {
      font-size: 0.78rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      text-decoration: none;
      color: var(--accent);
      border: 1px solid var(--accent);
      padding: 0.4rem 1rem;
      border-radius: 4px;
      transition: background 0.2s, color 0.2s;
    }
    .nav-cta:hover { background: var(--accent); color: #fff; }

    /* ─── Hero ───────────────────────────────────────────────────────── */
    .hero {
      min-height: 100vh;
      min-height: 100dvh;
      padding-top: var(--nav-h);
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      position: relative;
      overflow: hidden;
      /* Dark base + subtle diagonal hatching to suggest texture without an image */
      background-color: #080808;
      background-image:
        radial-gradient(ellipse 70% 60% at 15% 55%, rgba(185,28,28,0.09) 0%, transparent 65%),
        radial-gradient(ellipse 55% 45% at 85% 35%, rgba(185,28,28,0.06) 0%, transparent 60%),
        repeating-linear-gradient(
          -45deg,
          rgba(255,255,255,0.018) 0px,
          rgba(255,255,255,0.018) 1px,
          transparent 1px,
          transparent 9px
        );
    }

    /* bottom gradient fade to section below */
    .hero::after {
      content: '';
      position: absolute;
      bottom: 0; left: 0; right: 0;
      height: 180px;
      background: linear-gradient(to bottom, transparent, var(--bg-section));
      pointer-events: none;
    }

    .hero-body {
      position: relative;
      z-index: 2;
      padding: 4rem 1.5rem 5rem;
      max-width: 860px;
    }

    .hero h1 {
      font-size: clamp(3.2rem, 9vw, 7rem);
      font-weight: 900;
      letter-spacing: -0.025em;
      line-height: 0.95;
      text-transform: uppercase;
      /* Bright-to-silver gradient for metallic feel */
      background: linear-gradient(175deg, #FFFFFF 0%, #C8C8C8 55%, #8A8A8A 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 1.4rem;
    }

    .hero-sub {
      font-size: clamp(1rem, 2.5vw, 1.25rem);
      color: var(--text-secondary);
      letter-spacing: 0.04em;
      margin-bottom: 2.5rem;
    }
    .hero-sub strong { color: var(--text-primary); font-weight: 600; }

    .hero-btns {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    /* ─── Services ───────────────────────────────────────────────────── */
    .services {
      background: var(--bg-section);
      padding: 5.5rem 0;
    }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.25rem;
    }

    .service-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 2rem 1.75rem;
      position: relative;
      overflow: hidden;
      transition: transform 0.25s, border-color 0.25s, box-shadow 0.25s;
    }

    /* top accent bar that slides in on hover */
    .service-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 2px;
      background: var(--accent);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.3s ease;
    }

    .service-card:hover {
      transform: translateY(-5px);
      border-color: var(--border-hover);
      box-shadow: 0 20px 48px rgba(0,0,0,0.5);
    }
    .service-card:hover::before { transform: scaleX(1); }

    .service-icon {
      width: 36px;
      height: 36px;
      margin-bottom: 1.25rem;
    }
    .service-icon svg {
      width: 100%;
      height: 100%;
      stroke: var(--accent);
      fill: none;
      stroke-width: 1.75;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .service-name {
      font-size: 1rem;
      font-weight: 700;
      letter-spacing: 0.02em;
      margin-bottom: 0.5rem;
    }

    .service-desc {
      font-size: 0.85rem;
      color: var(--text-secondary);
      line-height: 1.7;
      margin-bottom: 1.25rem;
    }

    .service-price {
      font-size: 1rem;
      font-weight: 800;
      color: var(--accent);
      letter-spacing: 0.02em;
    }

    /* ─── Why Choose Us ──────────────────────────────────────────────── */
    .why {
      background: var(--bg-alt);
      padding: 5.5rem 0;
    }

    .why-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 1.25rem;
    }

    .why-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 2.25rem 1.5rem;
      text-align: center;
      transition: border-color 0.25s, box-shadow 0.25s;
    }
    .why-card:hover {
      border-color: var(--border-hover);
      box-shadow: 0 8px 32px rgba(0,0,0,0.4);
    }

    .why-badge {
      width: 52px;
      height: 52px;
      border-radius: 50%;
      background: rgba(185,28,28,0.1);
      border: 1px solid rgba(185,28,28,0.18);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.25rem;
    }
    .why-badge svg {
      width: 22px; height: 22px;
      stroke: var(--accent); fill: none;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .why-title {
      font-size: 0.95rem;
      font-weight: 700;
      letter-spacing: 0.02em;
      margin-bottom: 0.5rem;
    }

    .why-desc {
      font-size: 0.82rem;
      color: var(--text-secondary);
      line-height: 1.7;
    }

    /* ─── Contact ────────────────────────────────────────────────────── */
    .contact {
      background: var(--bg-section);
      padding: 5.5rem 0;
    }

    .contact-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 5rem;
      align-items: start;
    }

    .contact-phone-link {
      display: block;
      font-size: clamp(2.2rem, 5vw, 3.75rem);
      font-weight: 900;
      letter-spacing: -0.03em;
      line-height: 1;
      text-decoration: none;
      color: var(--text-primary);
      transition: color 0.2s;
      margin-bottom: 0.3rem;
    }
    .contact-phone-link:hover { color: var(--accent); }

    .tap-label {
      font-size: 0.68rem;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 2.75rem;
    }

    .info-block { margin-bottom: 2rem; }

    .info-label {
      font-size: 0.67rem;
      font-weight: 700;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 0.55rem;
    }

    .info-block p {
      font-size: 0.9rem;
      color: var(--text-secondary);
      line-height: 1.85;
    }

    .hours-table { width: 100%; border-collapse: collapse; }
    .hours-table tr { border-bottom: 1px solid var(--border); }
    .hours-table tr:last-child { border-bottom: none; }
    .hours-table td {
      padding: 0.45rem 0;
      font-size: 0.88rem;
    }
    .hours-table td:first-child { color: var(--text-secondary); }
    .hours-table td:last-child {
      text-align: right;
      font-weight: 500;
      color: var(--text-primary);
    }
    .hours-table .closed { color: var(--text-muted); }

    /* Map placeholder */
    .map-wrap {
      border-radius: var(--radius);
      overflow: hidden;
      border: 1px solid var(--border);
      background: var(--bg-card);
      aspect-ratio: 4 / 3;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 0.9rem;
      color: var(--text-muted);
    }
    .map-wrap svg {
      width: 44px; height: 44px;
      stroke: var(--text-muted); fill: none;
      stroke-width: 1.5;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    .map-wrap p {
      font-size: 0.82rem;
      letter-spacing: 0.05em;
      text-align: center;
      line-height: 1.6;
    }
    .map-wrap .map-hint {
      font-size: 0.72rem;
      color: var(--text-muted);
      opacity: 0.6;
    }

    /* ─── Footer ─────────────────────────────────────────────────────── */
    footer {
      background: #080808;
      border-top: 1px solid var(--border);
      padding: 3rem 2rem;
      /* Extra room so mobile sticky bar doesn't overlap footer text */
      padding-bottom: calc(var(--mobile-bar-h) + 1.5rem);
    }

    @media (min-width: 769px) {
      footer { padding-bottom: 3rem; }
    }

    .footer-inner {
      max-width: 1180px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      flex-wrap: wrap;
      gap: 2rem;
    }

    .footer-brand {
      font-size: 1.05rem;
      font-weight: 800;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      margin-bottom: 0.4rem;
    }
    .footer-brand em { color: var(--accent); font-style: normal; }

    .footer-tagline {
      font-size: 0.78rem;
      color: var(--text-muted);
      letter-spacing: 0.05em;
    }

    .footer-details {
      text-align: right;
    }
    .footer-details p {
      font-size: 0.82rem;
      color: var(--text-muted);
      line-height: 1.9;
    }
    .footer-details a {
      text-decoration: none;
      color: var(--text-secondary);
      transition: color 0.2s;
    }
    .footer-details a:hover { color: var(--accent); }

    .footer-copy {
      max-width: 1180px;
      margin: 1.75rem auto 0;
      padding-top: 1.5rem;
      border-top: 1px solid var(--border);
      font-size: 0.72rem;
      color: var(--text-muted);
      letter-spacing: 0.06em;
    }

    /* ─── Sticky CTA — Mobile Bar ────────────────────────────────────── */
    .mobile-bar {
      display: none;
      position: fixed;
      bottom: 0; left: 0; right: 0;
      z-index: 900;
      height: var(--mobile-bar-h);
      background: #0C0C0C;
      border-top: 1px solid rgba(255,255,255,0.1);
      padding: 0.6rem 1rem;
      gap: 0.65rem;
      align-items: center;
    }

    .mobile-bar a {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100%;
      font-size: 0.85rem;
      font-weight: 700;
      letter-spacing: 0.09em;
      text-transform: uppercase;
      text-decoration: none;
      border-radius: 4px;
    }

    .mbar-call {
      background: var(--accent);
      color: #fff;
    }
    .mbar-call:active { background: var(--accent-dark); }

    .mbar-quote {
      background: transparent;
      color: #fff;
      border: 2px solid rgba(255,255,255,0.22);
    }
    .mbar-quote:active { background: rgba(255,255,255,0.08); }

    /* ─── Sticky CTA — Desktop Float ────────────────────────────────── */
    .desktop-float {
      display: none;
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      z-index: 900;
    }

    .desktop-float a {
      display: flex;
      align-items: center;
      gap: 0.55rem;
      background: var(--accent);
      color: #fff;
      padding: 0.9rem 1.6rem;
      border-radius: 4px;
      font-size: 0.82rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      text-decoration: none;
      box-shadow: 0 8px 30px var(--accent-glow);
      transition: background 0.2s, transform 0.15s, box-shadow 0.2s;
    }
    .desktop-float a:hover {
      background: var(--accent-dark);
      transform: translateY(-2px);
      box-shadow: 0 14px 40px rgba(185,28,28,0.55);
    }
    .desktop-float svg {
      width: 15px; height: 15px;
      stroke: currentColor; fill: none;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
      flex-shrink: 0;
    }

    /* ─── Responsive Breakpoints ─────────────────────────────────────── */
    @media (max-width: 1024px) {
      .services-grid { grid-template-columns: repeat(2, 1fr); }
      .why-grid      { grid-template-columns: repeat(2, 1fr); }
    }

    @media (max-width: 768px) {
      /* Show mobile bar */
      .mobile-bar    { display: flex; }
      .desktop-float { display: none; }

      /* Hide nav links on mobile; keep logo + cta number */
      .nav-links { display: none; }
      .nav { padding: 0 1.25rem; }

      .hero-body { padding-bottom: 6rem; } /* clear mobile bar */

      .services-grid { grid-template-columns: 1fr; }
      .why-grid      { grid-template-columns: 1fr 1fr; }
      .contact-grid  { grid-template-columns: 1fr; gap: 2.5rem; }

      .footer-inner   { flex-direction: column; }
      .footer-details { text-align: left; }

      section, .services, .why, .contact { padding: 4rem 0; }
      .container { padding: 0 1.25rem; }
    }

    @media (min-width: 769px) {
      /* Show desktop float */
      .mobile-bar    { display: none; }
      .desktop-float { display: block; }
    }

    @media (max-width: 480px) {
      .why-grid { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>

<!-- ════════════════════════════════════════════
     NAVIGATION
════════════════════════════════════════════ -->
<nav class="nav">
  <div class="nav-inner">
    <a href="#" class="nav-logo">AUTO SHOP <em>NAME</em></a>

    <ul class="nav-links">
      <li><a href="#services">Services</a></li>
      <li><a href="#why">Why Us</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>

    <a href="tel:718-XXX-XXXX" class="nav-cta">718-XXX-XXXX</a>
  </div>
</nav>


<!-- ════════════════════════════════════════════
     HERO SECTION
════════════════════════════════════════════ -->
<section class="hero" id="home">
  <div class="hero-body">
    <p class="eyebrow">Full-Service Auto Repair</p>
    <h1>AUTO SHOP NAME</h1>
    <p class="hero-sub">Trusted Auto Repair in <strong>[Neighborhood Name]</strong></p>
    <div class="hero-btns">
      <a href="tel:718-XXX-XXXX" class="btn btn-primary">Call Now</a>
      <a href="#contact"         class="btn btn-ghost">Get a Quote</a>
    </div>
  </div>
</section>


<!-- ════════════════════════════════════════════
     SERVICES SECTION
════════════════════════════════════════════ -->
<section class="services" id="services">
  <div class="container">
    <p class="eyebrow">What We Do</p>
    <h2 class="section-title">Our Services</h2>
    <div class="rule"></div>

    <div class="services-grid">

      <!-- Oil Change -->
      <div class="service-card">
        <div class="service-icon">
          <svg viewBox="0 0 24 24">
            <path d="M12 2a5 5 0 0 0-5 5c0 3.5 5 11 5 11s5-7.5 5-11a5 5 0 0 0-5-5z"/>
            <circle cx="12" cy="7" r="2"/>
          </svg>
        </div>
        <div class="service-name">Oil Change</div>
        <p class="service-desc">Full synthetic or conventional oil change with a multi-point vehicle inspection included.</p>
        <div class="service-price">Starting at $59</div>
      </div>

      <!-- Brake Service -->
      <div class="service-card">
        <div class="service-icon">
          <svg viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10"/>
            <circle cx="12" cy="12" r="4"/>
            <line x1="12" y1="2" x2="12" y2="6"/>
            <line x1="12" y1="18" x2="12" y2="22"/>
            <line x1="2" y1="12" x2="6" y2="12"/>
            <line x1="18" y1="12" x2="22" y2="12"/>
          </svg>
        </div>
        <div class="service-name">Brake Service</div>
        <p class="service-desc">Pad replacement and rotor resurfacing or replacement for reliable, confident stopping power.</p>
        <div class="service-price">Starting at $189</div>
      </div>

      <!-- Alternator Replacement -->
      <div class="service-card">
        <div class="service-icon">
          <svg viewBox="0 0 24 24">
            <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>
          </svg>
        </div>
        <div class="service-name">Alternator Replacement</div>
        <p class="service-desc">Diagnose and replace a failing alternator to keep your charging system running properly.</p>
        <div class="service-price">Starting at $749</div>
      </div>

      <!-- Tire Rotation & Balance -->
      <div class="service-card">
        <div class="service-icon">
          <svg viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10"/>
            <circle cx="12" cy="12" r="3"/>
            <line x1="12" y1="2" x2="12" y2="9"/>
            <line x1="12" y1="15" x2="12" y2="22"/>
            <line x1="2" y1="12" x2="9" y2="12"/>
            <line x1="15" y1="12" x2="22" y2="12"/>
          </svg>
        </div>
        <div class="service-name">Tire Rotation &amp; Balance</div>
        <p class="service-desc">Extend tire life and smooth out your ride with a full rotation and dynamic wheel balance.</p>
        <div class="service-price">Starting at $49</div>
      </div>

      <!-- NY State Inspection -->
      <div class="service-card">
        <div class="service-icon">
          <svg viewBox="0 0 24 24">
            <path d="M9 11l3 3L22 4"/>
            <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/>
          </svg>
        </div>
        <div class="service-name">NY State Inspection</div>
        <p class="service-desc">Official New York State safety and emissions inspection performed by certified technicians.</p>
        <div class="service-price">$37</div>
      </div>

      <!-- Check Engine Diagnostic -->
      <div class="service-card">
        <div class="service-icon">
          <svg viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10"/>
            <line x1="12" y1="8" x2="12" y2="12"/>
            <line x1="12" y1="16" x2="12.01" y2="16"/>
          </svg>
        </div>
        <div class="service-name">Check Engine Diagnostic</div>
        <p class="service-desc">Computer scan and full diagnostic to pinpoint the root cause of your check engine light.</p>
        <div class="service-price">Starting at $99</div>
      </div>

    </div><!-- /services-grid -->
  </div><!-- /container -->
</section>


<!-- ════════════════════════════════════════════
     WHY CHOOSE US SECTION
════════════════════════════════════════════ -->
<section class="why" id="why">
  <div class="container">
    <p class="eyebrow">Our Promise</p>
    <h2 class="section-title">Why Choose Us</h2>
    <div class="rule"></div>

    <div class="why-grid">

      <!-- Honest Pricing -->
      <div class="why-card">
        <div class="why-badge">
          <svg viewBox="0 0 24 24">
            <line x1="12" y1="1" x2="12" y2="23"/>
            <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
          </svg>
        </div>
        <div class="why-title">Honest Pricing</div>
        <p class="why-desc">No upsells, no surprises. You get a clear written estimate before any work begins.</p>
      </div>

      <!-- Family Owned -->
      <div class="why-card">
        <div class="why-badge">
          <svg viewBox="0 0 24 24">
            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
            <polyline points="9 22 9 12 15 12 15 22"/>
          </svg>
        </div>
        <div class="why-title">Family Owned</div>
        <p class="why-desc">Serving the neighborhood for years with the care and accountability of a family business.</p>
      </div>

      <!-- Same-Day Service -->
      <div class="why-card">
        <div class="why-badge">
          <svg viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
        </div>
        <div class="why-title">Same-Day Service</div>
        <p class="why-desc">Most repairs are completed in a single visit so you can get back on the road fast.</p>
      </div>

      <!-- All Makes & Models -->
      <div class="why-card">
        <div class="why-badge">
          <svg viewBox="0 0 24 24">
            <rect x="1" y="3" width="15" height="13" rx="2"/>
            <path d="M16 8h4l3 5v3h-7V8z"/>
            <circle cx="5.5" cy="18.5" r="2.5"/>
            <circle cx="18.5" cy="18.5" r="2.5"/>
          </svg>
        </div>
        <div class="why-title">All Makes &amp; Models</div>
        <p class="why-desc">Domestic, Japanese, German, Korean — our technicians are trained to handle them all.</p>
      </div>

    </div><!-- /why-grid -->
  </div><!-- /container -->
</section>


<!-- ════════════════════════════════════════════
     CONTACT / LOCATION SECTION
════════════════════════════════════════════ -->
<section class="contact" id="contact">
  <div class="container">
    <p class="eyebrow">Find Us</p>
    <h2 class="section-title">Contact &amp; Location</h2>
    <div class="rule"></div>

    <div class="contact-grid">

      <!-- Info column -->
      <div>
        <a href="tel:718-XXX-XXXX" class="contact-phone-link">718-XXX-XXXX</a>
        <p class="tap-label">Tap to call</p>

        <div class="info-block">
          <p class="info-label">Address</p>
          <p>123 Main St<br>Flushing, NY 11354</p>
        </div>

        <div class="info-block">
          <p class="info-label">Hours</p>
          <table class="hours-table">
            <tr>
              <td>Mon &ndash; Fri</td>
              <td>8:00 AM &ndash; 6:00 PM</td>
            </tr>
            <tr>
              <td>Saturday</td>
              <td>8:00 AM &ndash; 3:00 PM</td>
            </tr>
            <tr>
              <td>Sunday</td>
              <td class="closed">Closed</td>
            </tr>
          </table>
        </div>

        <a href="tel:718-XXX-XXXX" class="btn btn-primary" style="margin-top:1.25rem;">Call Now</a>
      </div>

      <!-- Map column -->
      <div class="map-wrap">
        <svg viewBox="0 0 24 24">
          <path d="M21 10c0 7-9 13-9 13S3 17 3 10a9 9 0 0 1 18 0z"/>
          <circle cx="12" cy="10" r="3"/>
        </svg>
        <p>Google Maps Embed</p>
        <p class="map-hint">Replace this block with an &lt;iframe&gt; embed from Google Maps</p>
      </div>

    </div><!-- /contact-grid -->
  </div><!-- /container -->
</section>


<!-- ════════════════════════════════════════════
     FOOTER
════════════════════════════════════════════ -->
<footer>
  <div class="footer-inner">
    <div>
      <div class="footer-brand">AUTO SHOP <em>NAME</em></div>
      <p class="footer-tagline">Trusted Auto Repair in [Neighborhood Name]</p>
    </div>
    <div class="footer-details">
      <p>
        <a href="tel:718-XXX-XXXX">718-XXX-XXXX</a><br>
        123 Main St, Flushing, NY 11354<br>
        Mon&ndash;Fri 8am&ndash;6pm &nbsp;&bull;&nbsp; Sat 8am&ndash;3pm
      </p>
    </div>
  </div>
  <p class="footer-copy">&copy; 2025 AUTO SHOP NAME. All rights reserved.</p>
</footer>


<!-- ════════════════════════════════════════════
     STICKY CTA — MOBILE BAR
════════════════════════════════════════════ -->
<div class="mobile-bar" aria-label="Quick actions">
  <a href="tel:718-XXX-XXXX" class="mbar-call">Call Now</a>
  <a href="#contact"          class="mbar-quote">Get a Quote</a>
</div>


<!-- ════════════════════════════════════════════
     STICKY CTA — DESKTOP FLOAT
════════════════════════════════════════════ -->
<div class="desktop-float" aria-label="Call now">
  <a href="tel:718-XXX-XXXX">
    <svg viewBox="0 0 24 24">
      <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12a19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 3.62 1.27h3a2 2 0 0 1 2 1.72c.128.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.91a16 16 0 0 0 6.06 6.06l.91-.91a2 2 0 0 1 2.11-.45c.907.339 1.85.572 2.81.7A2 2 0 0 1 22 16.92z"/>
    </svg>
    Call Now
  </a>
</div>

</body>
</html>